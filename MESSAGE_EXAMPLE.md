# راهنمای استفاده از سیستم پیام‌های Django

## معرفی

سیستم پیام‌های Django در این پروژه با آیکون‌های زیبا، رنگ‌بندی مناسب و انیمیشن‌های smooth پیاده‌سازی شده است.

## انواع پیام‌ها

### ۱. پیام موفقیت (Success)
```python
from django.contrib import messages

def my_view(request):
    # عملیات موفق
    messages.success(request, "عملیات با موفقیت انجام شد!")
    return redirect('home')
```

### ۲. پیام خطا (Error)
```python
def my_view(request):
    # خطا در عملیات
    messages.error(request, "متاسفانه خطایی رخ داده است. لطفا دوباره تلاش کنید.")
    return redirect('home')
```

### ۳. پیام هشدار (Warning)
```python
def my_view(request):
    # هشدار برای کاربر
    messages.warning(request, "توجه: این عملیات قابل بازگشت نیست!")
    return redirect('home')
```

### ۴. پیام اطلاعاتی (Info)
```python
def my_view(request):
    # اطلاعات مفید برای کاربر
    messages.info(request, "اطلاعات شما با موفقیت ذخیره شد.")
    return redirect('home')
```

### ۵. پیام دیباگ (Debug)
```python
def my_view(request):
    # فقط در حالت DEBUG نمایش داده می‌شود
    messages.debug(request, "متغیر x برابر است با: " + str(x))
    return redirect('home')
```

## ویژگی‌های پیام‌ها

### ✨ ویژگی‌ها:
- **آیکون‌های FontAwesome**: هر نوع پیام آیکون مخصوص خود را دارد
- **رنگ‌بندی مناسب**: رنگ‌های semantic برای هر نوع پیام
- **انیمیشن smooth**: ورود و خروج پیام‌ها با انیمیشن
- **دکمه بستن**: کاربران می‌توانند پیام‌ها را ببندند
- **خودکار مخفی شدن**: پیام‌ها پس از ۵ ثانیه به طور خودکار محو می‌شوند
- **Responsive**: در موبایل و دسکتاپ به خوبی نمایش داده می‌شوند

### 🎨 آیکون‌های استفاده شده:
- **Success**: `fas fa-check-circle` (سبز)
- **Error**: `fas fa-exclamation-triangle` (قرمز)
- **Warning**: `fas fa-exclamation-circle` (زرد)
- **Info**: `fas fa-info-circle` (آبی)
- **Debug**: `fas fa-bug` (بنفش)

## تنظیمات (Settings)

در فایل `config/settings.py` تنظیمات زیر اضافه شده:

```python
MESSAGE_TAGS = {
    'DEBUG': 'debug',
    'INFO': 'info',
    'SUCCESS': 'success',
    'WARNING': 'warning',
    'ERROR': 'error',
}
```

## استفاده در Template

پیام‌ها به طور خودکار در `_base.html` نمایش داده می‌شوند. نیازی به تغییر در templateهای دیگر نیست.

## مثال عملی

```python
from django.shortcuts import redirect
from django.contrib import messages

def create_user(request):
    if request.method == 'POST':
        # پردازش فرم
        if form.is_valid():
            user = form.save()
            messages.success(request, f"کاربر {user.username} با موفقیت ایجاد شد!")
            return redirect('user_list')
        else:
            messages.error(request, "لطفا خطاهای فرم را برطرف کنید.")
    return render(request, 'create_user.html', {'form': form})
```

## نکات مهم

1. پیام‌ها در session ذخیره می‌شوند و پس از نمایش پاک می‌شوند
2. هر پیام حداکثر یک بار نمایش داده می‌شود
3. پیام‌ها به زبان پارسی هستند و از راست به چپ نمایش داده می‌شوند
4. در موبایل پیام‌ها responsive هستند و اندازه‌شان تغییر می‌کند
